<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- TextView for Email (non-editable) -->


    <!-- EditText for Name -->
    <EditText
        android:id="@+id/user_nameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Name" />

    <!-- EditText for Username -->
    <EditText
        android:id="@+id/user_usernameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Username" />

    <!-- EditText for Password -->
    <EditText
        android:id="@+id/user_passwordEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Password"
        android:inputType="textPassword" />

    <!-- EditText for Company -->
    <EditText
        android:id="@+id/user_companyEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Company" />

    <!-- EditText for Street -->
    <EditText
        android:id="@+id/user_streetEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Street" />

    <!-- EditText for Zip -->
    <EditText
        android:id="@+id/user_zipEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Zip" />

    <!-- EditText for City -->
    <EditText
        android:id="@+id/user_cityEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="City" />

    <!-- EditText for Country -->
    <EditText
        android:id="@+id/user_countryEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Country" />

    <!-- EditText for Phone -->
    <EditText
        android:id="@+id/user_phoneEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Phone" />

    <!-- EditText for Company Business -->
    <EditText
        android:id="@+id/user_companyBusinessEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Company Business" />

    <!-- EditText for Company Phrase -->
    <EditText
        android:id="@+id/user_companyPhraseEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Company Phrase" />

    <TextView
        android:id="@+id/user_emailTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        android:text="Email: " />

    <!-- TextView for Status (non-editable) -->
    <TextView
        android:id="@+id/user_statusTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        android:text="Status: " />

    <Button
        android:id="@+id/updatedata_user2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Update"
        android:layout_margin="5dp"/>




</LinearLayout>
